[Unit]
Description=Send the Portfolio AI heartbeat once a day

[Timer]
# 12:00 UTC ≈ 08:00 America/New_York. Persistent=true so a missed run (VPS was off)
# fires on next boot.
OnCalendar=*-*-* 12:00:00
Persistent=true

[Install]
WantedBy=timers.target
